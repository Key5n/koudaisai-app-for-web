import { FC } from "react";
import styles from "styles/reservation.module.css";

const MoveButton: FC = () => {
  return <button className={styles["button--move"]}>テスト</button>;
};

export default MoveButton;
