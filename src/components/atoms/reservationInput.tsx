import { FC } from "react";
import styles from "styles/reservation.module.css";

const ReservationInput: FC = () => {
  return <input className={styles["input--item"]}></input>;
};

export default ReservationInput;
